# Game of Life

A small, object-oriented implementation of Conway's Game of Life.

## Run locally

```bash
composer dump-autoload
php -S localhost:8000
```

Open `http://localhost:8000` in a browser. Board dimensions and generation count
are defined once in `config/config.php`.
